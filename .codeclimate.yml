version: "2"
plugins:
    similar-code:
        config:
           threshold: 88
    identical-code:
       config:
          threshold: 50
checks:
   method-lines:
      config:
         threshold: 30
   argument-count:
      config:
         threshold: 7
